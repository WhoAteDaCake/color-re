module Color = ColorRe;
/* let tap = n => {
     Js.log(n);
     n;
   };

   let hsl =
     Color.rgbToHsl(Some((0, 100, 50)))
     |> Color.hslToRgb
     |> Color.rgbToHsl
     |> Color.hslToRgb;

   switch hsl {
   | None => ()
   | Some((h, s, l)) =>
     Js.log((0, 100, 50));
     Js.log((h, s, l));
   }; */